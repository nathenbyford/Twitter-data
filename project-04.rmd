---
title: "Project 4"
author: "Nathen Byford"
date: "12/2/2021"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, collapse = TRUE)
library("tidyverse")
theme_set(
  theme_classic() +
  theme(panel.grid.major.y = element_line(),
    axis.title.y = element_text(angle = 0, vjust = .5)
  )
)
```

# Load data

```{r}
dtweets <- read_rds(".//data//dtweets.rds")
```

# Question 2

```{r, fig.height=3.5, fig.align='center'}
dtweets |> 
  mutate(length = str_length(text)) |> 
    ggplot() +
    geom_histogram(aes(x=length), fill = "tan1", color = "cornsilk1", binwidth =5) +
    #Color choices inspired by the color of Donald Trump's hair and face
    labs(title = "Bimodal distribution of Donald Trump's Tweet Length") +
    xlab("Length of Tweet in Characters")+
    ylab("Number of Tweets") +
    theme_dark() + #Theme chosen to emphasize color choice
    theme(panel.grid.major.x = element_blank(), panel.grid.minor.x = element_blank())
```


# Question 3

```{r}
good_grade <- "The correct thing here"
```

# Question 4

```{r}
mentions <- dtweets |> 
  pull(text) |> 
  str_extract_all(pattern = "\\@\\w{4,15}")

map(mentions, str_split, pattern = ", ") |> 
  unlist() |> 
  as_tibble() |> 
  group_by(value) |> 
  count() |> 
  ungroup() |> 
  arrange(desc(n))

```

